spring:
  datasource:
    username: beer_inventory_service
    password: password
    url: jdbc:mysql://172.18.0.5:3306/beerinventoryservice?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC

# Enable logging for config troubleshooting
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
      type:
        descriptor:
          sql:
            BasicBinder: TRACE
    com:
      zaxxer:
        hikari:
          HikariConfig: DEBUG